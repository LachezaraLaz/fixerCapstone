
async function initChat(issueTitle, clientId, professionalId) {

    console.log(`Initializing chat for Issue: ${issueTitle}, 
                 Client: ${clientId}, 
                 Professional: ${professionalId}`);
}

module.exports = { initChat };
